import { style } from "@vanilla-extract/css";

export default {
    container: style({
        display: 'flex',
        height: 22,
        backgroundColor: '#af48ae',
        color: "white",
        border: 0,
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 4,
        cursor: 'pointer'
    })
}